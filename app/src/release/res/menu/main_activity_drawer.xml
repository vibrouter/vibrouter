<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/start_stop_navigation"
            android:icon="@drawable/ic_menu_gallery"
            android:title="@string/start_navigation" />
        <item
            android:id="@+id/go_to_current_position"
            android:icon="@drawable/ic_menu_slideshow"
            android:title="@string/goto_current_position" />
        <item
            android:id="@+id/enable_camera_reset"
            android:icon="@drawable/ic_menu_manage"
            android:title="@string/enable_camera_position_reset" />
    </group>

    <item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send" />
        </menu>
    </item>

</menu>
